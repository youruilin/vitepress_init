# 实用版全量数据入库流程

## 爬数据

当前python文件，支持增量爬取，具有以下特征：

✔ 支持每页50条数据（官方允许的最大请求页码）

✔ 按批次分组存储（每100页一个文件）

✔ 支持5万+数据分批爬取

✔ 内存友好，管理简单

✔ 支持断点续传

✔ 随机间隔1-10秒，模拟人类行为，避免服务器压力

✔ 增量爬取 - 单独存储新增数据，支持多次增量检测

```python
# -*- coding: utf-8 -*-
"""
种业数据爬虫（优化分批版 - 随机间隔版）
✔ 支持每页50条数据（最大）
✔ 按批次分组存储（每100页一个文件）
✔ 支持5万+数据分批爬取
✔ 内存友好，管理简单
✔ 支持断点续传
✔ 随机间隔1-10秒，模拟人类行为，避免服务器压力
✔ 增量爬取 - 单独存储新增数据，支持多次增量检测
"""

import requests
import json
import time
import random
from pathlib import Path
from datetime import datetime

BASE_URL = "http://202.127.42.144:60011"

# ================= 配置参数（重点！）=================
CONFIG = {
    "PAGE_SIZE": 50,               # 每页数据量（最大支持50条）
    "PAGES_PER_BATCH": 100,        # 每批次包含多少页（100页=5000条）
    "MIN_INTERVAL": 2,             # 最小请求间隔（秒）
    "MAX_INTERVAL": 5,             # 最大请求间隔（秒）
    "MAX_PAGES": 2000,             # 最大页数
}

# 自动计算派生参数
CONFIG["RECORDS_PER_BATCH"] = CONFIG["PAGES_PER_BATCH"] * CONFIG["PAGE_SIZE"]
CONFIG["TOTAL_RECORDS"] = CONFIG["MAX_PAGES"] * CONFIG["PAGE_SIZE"]
CONFIG["TOTAL_BATCHES"] = (CONFIG["MAX_PAGES"] + CONFIG["PAGES_PER_BATCH"] - 1) // CONFIG["PAGES_PER_BATCH"]

HEADERS = {
    "ApiToken": "EjJ46nMm3zY02FkmBz0fezyDq08L42uO",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36",
    "Accept": "application/json, text/plain, */*",
    "Referer": "http://202.127.42.144:60011/controlboard/",
}


# ================= 安全请求 =================
def safe_get(url, params=None):
    """安全请求，带重试机制"""
    for attempt in range(3):
        try:
            res = requests.get(url, params=params, headers=HEADERS, timeout=10)
            
            if res.status_code == 200:
                try:
                    return res.json()
                except:
                    return None
            else:
                if attempt < 2:
                    time.sleep(2)
                    continue
                return None
                
        except Exception as e:
            if attempt < 2:
                time.sleep(2)
                continue
            return None
    
    return None


# ================= 转换列式数据为行式数据 =================
def convert_column_to_row(column_data):
    """将列式存储的数据转换为行式存储"""
    if not column_data or not isinstance(column_data, dict):
        return []
    
    fields = list(column_data.keys())
    if not fields:
        return []
    
    first_field = fields[0]
    row_count = len(column_data[first_field])
    
    rows = []
    for i in range(row_count):
        row = {}
        for field in fields:
            value = column_data[field].get(str(i))
            row[field] = value
        rows.append(row)
    
    return rows


# ================= 获取单页数据 =================
def get_rowData(page=1, page_size=50):
    """获取单页数据"""
    url = f"{BASE_URL}/v1/data-service/api/getDwdSdVarietyannouncementList"

    params = {
        "page": page,
        "pageSize": page_size,
        "cropid": "",
        "judgementyear": "",
        "judgementregion": "",
        "varietyname": "",
        "judgementno": "",
        "companyName": "",
        "istransgenosis": "",
        "status": "",
        "introductionfilingnoSign": ""
    }

    data = safe_get(url, params)

    if not isinstance(data, dict):
        return []

    row_data = data.get("data", {}).get("rowData", [])
    
    if not row_data:
        return []

    if isinstance(row_data, dict):
        return convert_column_to_row(row_data)
    elif isinstance(row_data, list):
        return row_data
    
    return []


# ================= 分批存储管理器 =================
class BatchStorage:
    """分批存储管理器"""
    
    def __init__(self, data_dir="variety_batch_data"):
        self.data_dir = Path(data_dir)
        self.data_dir.mkdir(exist_ok=True)
        self.pages_per_batch = CONFIG["PAGES_PER_BATCH"]
        self.progress_file = self.data_dir / "progress.json"
        self.progress = self._load_progress()
    
    def _get_batch_name(self, page_num):
        batch_index = (page_num - 1) // self.pages_per_batch + 1
        start_page = (batch_index - 1) * self.pages_per_batch + 1
        end_page = min(batch_index * self.pages_per_batch, CONFIG["MAX_PAGES"])
        return f"batch_{batch_index:03d}_pages_{start_page:04d}-{end_page:04d}.json"
    
    def _get_batch_index(self, page_num):
        return (page_num - 1) // self.pages_per_batch + 1
    
    def _load_progress(self):
        if self.progress_file.exists():
            with open(self.progress_file, 'r', encoding='utf-8') as f:
                progress = json.load(f)
                print(f"📂 加载进度: 已爬取 {progress.get('total_records', 0):,} 条, 最后页码 {progress.get('last_page', 0)}")
                return progress
        
        return {
            "version": "3.0",
            "start_time": datetime.now().isoformat(),
            "last_page": 0,
            "total_records": 0,
            "crawled_pages": [],
            "batches": {}
        }
    
    def _save_progress(self):
        self.progress["last_update"] = datetime.now().isoformat()
        with open(self.progress_file, 'w', encoding='utf-8') as f:
            json.dump(self.progress, f, indent=2, ensure_ascii=False)
    
    def _load_batch_file(self, batch_file):
        file_path = self.data_dir / batch_file
        if not file_path.exists():
            return {}
        with open(file_path, 'r', encoding='utf-8') as f:
            return json.load(f)
    
    def _save_batch_file(self, batch_file, data):
        file_path = self.data_dir / batch_file
        with open(file_path, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        
        file_size = file_path.stat().st_size / (1024 * 1024)
        print(f"  💾 批次文件已更新: {batch_file} ({file_size:.2f}MB)")
    
    def save_page(self, page_num, page_data):
        if not page_data:
            return False
        
        batch_file = self._get_batch_name(page_num)
        batch_index = self._get_batch_index(page_num)
        
        batch_data = self._load_batch_file(batch_file)
        batch_data[str(page_num)] = page_data
        self._save_batch_file(batch_file, batch_data)
        
        if page_num not in self.progress["crawled_pages"]:
            self.progress["crawled_pages"].append(page_num)
        
        self.progress["last_page"] = page_num
        self.progress["total_records"] += len(page_data)
        
        if batch_index not in self.progress["batches"]:
            self.progress["batches"][batch_index] = {
                "file": batch_file,
                "pages": [],
                "records": 0,
                "start_page": (batch_index - 1) * self.pages_per_batch + 1,
                "end_page": min(batch_index * self.pages_per_batch, CONFIG["MAX_PAGES"])
            }
        
        batch_info = self.progress["batches"][batch_index]
        if page_num not in batch_info["pages"]:
            batch_info["pages"].append(page_num)
        batch_info["records"] += len(page_data)
        
        self._save_progress()
        
        print(f"  ✅ 第{page_num}页已存入: {batch_file}")
        print(f"  📊 本批次: {len(batch_info['pages'])}/{batch_info['end_page']-batch_info['start_page']+1}页, {batch_info['records']:,}/{CONFIG['RECORDS_PER_BATCH']:,}条")
        print(f"  📊 全局: {self.progress['total_records']:,}/{CONFIG['TOTAL_RECORDS']:,}条 ({self.progress['total_records']/CONFIG['TOTAL_RECORDS']*100:.1f}%)")
        
        return True
    
    def is_page_crawled(self, page_num):
        return page_num in self.progress["crawled_pages"]
    
    def get_next_page(self, start_page=1):
        for page in range(start_page, start_page + 10000):
            if not self.is_page_crawled(page):
                return page
        return None
    
    def get_statistics(self):
        total_batches = len(self.progress["batches"])
        total_pages = len(self.progress["crawled_pages"])
        total_records = self.progress["total_records"]
        
        total_size = 0
        for batch_info in self.progress["batches"].values():
            file_path = self.data_dir / batch_info["file"]
            if file_path.exists():
                total_size += file_path.stat().st_size
        
        completion_rate = (total_records / CONFIG["TOTAL_RECORDS"] * 100) if CONFIG["TOTAL_RECORDS"] > 0 else 0
        
        return {
            "total_batches": total_batches,
            "total_pages": total_pages,
            "total_records": total_records,
            "expected_records": CONFIG["TOTAL_RECORDS"],
            "completion_rate": completion_rate,
            "last_page": self.progress["last_page"],
            "total_size_mb": total_size / (1024 * 1024),
            "data_dir": str(self.data_dir),
        }
    
    def get_all_batches(self):
        return sorted(self.progress["batches"].keys())
    
    def get_all_data_generator(self):
        for batch_index in self.get_all_batches():
            batch_info = self.progress["batches"][batch_index]
            batch_data = self._load_batch_file(batch_info["file"])
            for page_num in sorted(batch_data.keys(), key=int):
                for record in batch_data[page_num]:
                    yield record
    
    def get_base_judgementnos(self):
        """获取基准审定编号（来自第一个批次文件）"""
        batch_files = sorted(self.data_dir.glob("batch_*.json"))
        if not batch_files:
            return set()
        
        first_batch_file = batch_files[0]
        print(f"📂 读取基准批次文件: {first_batch_file.name}")
        
        with open(first_batch_file, 'r', encoding='utf-8') as f:
            batch_data = json.load(f)
        
        judgementnos = set()
        for page_num, page_data in batch_data.items():
            for record in page_data:
                judgementno = record.get('judgementno')
                if judgementno:
                    judgementnos.add(judgementno)
        
        print(f"  基准审定编号数: {len(judgementnos):,}")
        return judgementnos


# ================= 增量数据管理器 =================
class IncrementalManager:
    """增量数据管理器 - 单独存储增量数据"""
    
    def __init__(self, inc_dir="incremental_data"):
        self.inc_dir = Path(inc_dir)
        self.inc_dir.mkdir(exist_ok=True)
        self.index_file = self.inc_dir / "incremental_index.json"
        self.index = self._load_index()
    
    def _load_index(self):
        """加载增量数据索引"""
        if self.index_file.exists():
            with open(self.index_file, 'r', encoding='utf-8') as f:
                index = json.load(f)
                print(f"📂 加载增量索引: {len(index.get('incremental_files', []))} 个增量文件")
                return index
        
        return {
            "version": "1.0",
            "created_at": datetime.now().isoformat(),
            "incremental_files": [],  # 增量文件列表
            "all_judgementnos": set(),  # 所有已存储的审定编号（包括主数据+所有增量）
            "total_pages": 0,
            "total_records": 0
        }
    
    def _save_index(self):
        """保存增量数据索引"""
        # 转换set为列表以便JSON序列化
        index_copy = self.index.copy()
        index_copy["all_judgementnos"] = list(self.index["all_judgementnos"])
        
        with open(self.index_file, 'w', encoding='utf-8') as f:
            json.dump(index_copy, f, indent=2, ensure_ascii=False)
    
    def load_all_known_judgementnos(self, base_storage):
        """
        加载所有已知的审定编号（主数据 + 所有增量数据）
        用于检测新数据
        """
        all_judgementnos = set()
        
        # 1. 加载主数据的审定编号
        print(f"\n🔍 加载主数据审定编号...")
        base_judgementnos = base_storage.get_base_judgementnos()
        all_judgementnos.update(base_judgementnos)
        print(f"  主数据: {len(base_judgementnos):,} 个")
        
        # 2. 加载所有增量数据的审定编号
        if self.index.get("all_judgementnos"):
            # 从索引加载（快速）
            inc_judgementnos = set(self.index.get("all_judgementnos", []))
            all_judgementnos.update(inc_judgementnos)
            print(f"  增量数据(索引): {len(inc_judgementnos):,} 个")
        else:
            # 从文件加载（首次使用）
            print(f"  正在加载增量数据文件...")
            for inc_file_info in self.index.get("incremental_files", []):
                file_path = self.inc_dir / inc_file_info["filename"]
                if file_path.exists():
                    with open(file_path, 'r', encoding='utf-8') as f:
                        inc_data = json.load(f)
                    
                    for page_num, page_data in inc_data.items():
                        for record in page_data:
                            judgementno = record.get('judgementno')
                            if judgementno:
                                all_judgementnos.add(judgementno)
                    
                    print(f"    {inc_file_info['filename']}: {inc_file_info['records']} 条")
        
        # 更新索引
        self.index["all_judgementnos"] = all_judgementnos
        self._save_index()
        
        print(f"\n📊 总计已知审定编号: {len(all_judgementnos):,} 个")
        return all_judgementnos
    
    def save_incremental_data(self, new_pages_data, new_page_numbers):
        """
        保存增量数据到单独文件
        new_pages_data: {page_num: page_data, ...}
        new_page_numbers: 新数据的页码列表
        """
        timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
        inc_file = self.inc_dir / f"incremental_{timestamp}.json"
        
        # 保存增量数据
        inc_data = {
            "crawl_time": datetime.now().isoformat(),
            "pages": new_page_numbers,
            "total_pages": len(new_page_numbers),
            "total_records": len(new_page_numbers) * CONFIG["PAGE_SIZE"],
            "data": new_pages_data
        }
        
        with open(inc_file, 'w', encoding='utf-8') as f:
            json.dump(inc_data, f, indent=2, ensure_ascii=False)
        
        # 更新索引
        self.index["incremental_files"].append({
            "filename": inc_file.name,
            "time": datetime.now().isoformat(),
            "pages": new_page_numbers,
            "records": len(new_page_numbers) * CONFIG["PAGE_SIZE"]
        })
        
        # 更新已知审定编号集合
        for page_num, page_data in new_pages_data.items():
            for record in page_data:
                judgementno = record.get('judgementno')
                if judgementno:
                    self.index["all_judgementnos"].add(judgementno)
        
        self.index["total_pages"] += len(new_page_numbers)
        self.index["total_records"] += len(new_page_numbers) * CONFIG["PAGE_SIZE"]
        self._save_index()
        
        file_size = inc_file.stat().st_size / (1024 * 1024)
        print(f"\n💾 增量数据已保存: {inc_file}")
        print(f"   文件大小: {file_size:.2f} MB")
        print(f"   新增页数: {len(new_page_numbers)}")
        print(f"   新增记录: {len(new_page_numbers) * CONFIG['PAGE_SIZE']:,} 条")
        
        return inc_file
    
    def show_incremental_history(self):
        """显示增量爬取历史"""
        print(f"\n{'='*60}")
        print(f"📜 增量爬取历史")
        print(f"{'='*60}")
        
        if not self.index["incremental_files"]:
            print(f"  暂无增量爬取记录")
        else:
            print(f"  总计增量次数: {len(self.index['incremental_files'])}")
            print(f"  总计增量页数: {self.index['total_pages']}")
            print(f"  总计增量记录: {self.index['total_records']:,} 条")
            print(f"\n  详细记录:")
            for i, inc_file in enumerate(self.index["incremental_files"], 1):
                print(f"    {i}. {inc_file['time'][:19]} - {inc_file['pages']}页, {inc_file['records']}条")
        
        print(f"{'='*60}")
    
    def get_all_incremental_data_generator(self):
        """生成器：逐条返回所有增量数据"""
        for inc_file_info in self.index["incremental_files"]:
            file_path = self.inc_dir / inc_file_info["filename"]
            if file_path.exists():
                with open(file_path, 'r', encoding='utf-8') as f:
                    inc_data = json.load(f)
                
                for page_num, page_data in inc_data["data"].items():
                    for record in page_data:
                        yield record


# ================= 随机延迟函数 =================
def random_delay(page_num, total_pages, success_count):
    """随机延迟函数"""
    if page_num >= total_pages:
        return
    
    # 基础随机延迟 2-5 秒
    delay = random.uniform(CONFIG["MIN_INTERVAL"], CONFIG["MAX_INTERVAL"])
    
    # 每50页多休息一下
    if success_count > 0 and success_count % 50 == 0:
        rest_time = random.uniform(10, 20)
        print(f"\n  😴 已爬取 {success_count} 页，休息 {rest_time:.1f} 秒...")
        time.sleep(rest_time)
        return
    
    print(f"  ⏱️  等待 {delay:.2f} 秒后继续...")
    time.sleep(delay)


# ================= 分批爬取核心函数 =================
def crawl_batch(start_page=1, end_page=None):
    """分批爬取数据（带随机延迟）"""
    if end_page is None:
        end_page = CONFIG["MAX_PAGES"]
    
    storage = BatchStorage()
    
    print(f"\n{'='*70}")
    print(f"🚀 开始分批爬取任务（随机间隔模式）")
    print(f"{'='*70}")
    print(f"  配置信息:")
    print(f"    每页数量: {CONFIG['PAGE_SIZE']} 条")
    print(f"    每批页数: {CONFIG['PAGES_PER_BATCH']} 页")
    print(f"    每批记录: {CONFIG['RECORDS_PER_BATCH']:,} 条")
    print(f"  随机延迟:")
    print(f"    请求间隔: {CONFIG['MIN_INTERVAL']}-{CONFIG['MAX_INTERVAL']} 秒")
    print(f"    批次休息: 每50页休息10-20秒")
    print(f"  任务范围:")
    print(f"    起始页码: {start_page}")
    print(f"    结束页码: {end_page}")
    print(f"    预计总条数: {(end_page - start_page + 1) * CONFIG['PAGE_SIZE']:,} 条")
    print(f"    存储目录: {storage.data_dir}")
    print(f"{'='*70}\n")
    
    start_time = time.time()
    
    success_pages = 0
    empty_pages = 0
    failed_pages = 0
    
    for page in range(start_page, end_page + 1):
        # 检查是否已爬取
        if storage.is_page_crawled(page):
            print(f"⏭️  第{page}页已存在，跳过")
            continue
        
        print(f"\n📄 [进度: {page}/{end_page}] 正在爬取第 {page} 页...")
        
        try:
            data = get_rowData(page=page, page_size=CONFIG["PAGE_SIZE"])
            
            if not data:
                print(f"  ⚠️ 第{page}页无数据")
                empty_pages += 1
                if empty_pages >= 5:
                    print(f"\n⚠️ 连续{empty_pages}页无数据，已到达数据末尾")
                    print(f"   实际总页数: {success_pages}")
                    break
                # 无数据也等待一下
                if page < end_page:
                    time.sleep(random.uniform(1, 3))
                continue
            
            # 有数据，重置空页计数
            empty_pages = 0
            
            # 保存到批次文件
            storage.save_page(page, data)
            success_pages += 1
            
            # 随机延迟
            if page < end_page:
                random_delay(page, end_page, success_pages)
            
        except KeyboardInterrupt:
            print(f"\n\n⚠️ 用户中断，已保存进度到第{page-1}页")
            print(f"下次运行可选择「继续上次」")
            break
        except Exception as e:
            print(f"  ❌ 第{page}页爬取失败: {e}")
            failed_pages += 1
            time.sleep(5)
            continue
    
    elapsed_time = time.time() - start_time
    stats = storage.get_statistics()
    
    print(f"\n{'='*70}")
    print(f"🎉 爬取任务完成！")
    print(f"{'='*70}")
    print(f"  成功页数: {success_pages}")
    print(f"  空页数: {empty_pages}")
    print(f"  失败页数: {failed_pages}")
    print(f"  总记录数: {stats['total_records']:,} 条")
    print(f"  总批次数: {stats['total_batches']} 个文件")
    print(f"  总文件大小: {stats['total_size_mb']:.1f} MB")
    print(f"  耗时: {elapsed_time/60:.1f} 分钟 ({elapsed_time:.1f}秒)")
    print(f"  平均速度: {stats['total_records']/elapsed_time:.1f} 条/秒")
    print(f"  存储位置: {storage.data_dir}")
    print(f"{'='*70}")


# ================= 增量爬取函数 =================
def incremental_crawl():
    """
    增量爬取 - 只爬取新增数据，单独存储
    新数据排在前列，通过对比审定编号判断
    """
    storage = BatchStorage()
    inc_manager = IncrementalManager()
    
    print(f"\n{'='*60}")
    print(f"📊 增量爬取模式（单独存储）")
    print(f"{'='*60}")
    
    # 检查是否有主数据
    stats = storage.get_statistics()
    if stats['total_records'] == 0:
        print(f"\n❌ 未发现主数据，请先运行「完整爬取」")
        return
    
    print(f"\n📊 当前状态:")
    print(f"  主数据记录数: {stats['total_records']:,} 条")
    print(f"  主数据页数: {stats['total_pages']} 页")
    
    # 加载所有已知的审定编号（主数据 + 已有增量数据）
    print(f"\n🔍 正在加载已知审定编号...")
    known_judgementnos = inc_manager.load_all_known_judgementnos(storage)
    
    # 检测新数据
    print(f"\n🔍 检测新数据（从第1页开始）...")
    
    new_pages_data = {}
    new_page_numbers = []
    max_check_pages = 500  # 最多检查500页
    
    for page in range(1, max_check_pages + 1):
        print(f"  检查第 {page} 页...", end=" ")
        
        data = get_rowData(page=page, page_size=CONFIG["PAGE_SIZE"])
        
        if not data:
            print("无数据，停止检测")
            break
        
        # 检查这一页是否包含已知的审定编号
        has_known = False
        new_in_page = 0
        
        for record in data:
            judgementno = record.get('judgementno')
            if judgementno:
                if judgementno in known_judgementnos:
                    has_known = True
                else:
                    new_in_page += 1
        
        if has_known:
            if new_in_page > 0:
                # 新旧混合（边界页，理论上不应该发生）
                print(f"⚠️ 混合页 (新:{new_in_page}, 旧:{len(data)-new_in_page})")
                # 仍然保存新数据部分
                new_pages_data[str(page)] = data
                new_page_numbers.append(page)
            else:
                # 全是旧数据，边界已到
                print(f"全是旧数据，边界已找到")
            break
        else:
            # 全是新数据
            print(f"✨ 全新数据 ({len(data)}条)")
            new_pages_data[str(page)] = data
            new_page_numbers.append(page)
    
    if not new_page_numbers:
        print(f"\n✅ 没有发现新数据！数据已是最新。")
        return
    
    print(f"\n📊 检测结果:")
    print(f"  新数据页数: {len(new_page_numbers)} 页")
    print(f"  新数据记录数: {len(new_page_numbers) * CONFIG['PAGE_SIZE']:,} 条")
    print(f"  页码范围: 第 {new_page_numbers[0]} - {new_page_numbers[-1]} 页")
    
    # 预览新数据
    first_page_data = new_pages_data.get(str(new_page_numbers[0]), [])
    if first_page_data:
        print(f"\n📋 新数据预览（第1页前5条）:")
        for i, record in enumerate(first_page_data[:5], 1):
            judgementno = record.get('judgementno', '无')
            varietyname = record.get('varietyname', '无')
            print(f"    {i}. {judgementno} - {varietyname}")
    
    confirm = input(f"\n是否保存这 {len(new_page_numbers)} 页新数据？(y/n): ")
    if confirm != 'y':
        print("已取消")
        return
    
    # 保存增量数据
    inc_manager.save_incremental_data(new_pages_data, new_page_numbers)
    
    print(f"\n✅ 增量爬取完成！")
    print(f"   提示: 新数据已单独保存，下次增量检测会自动识别")


# ================= 查看进度 =================
def show_progress():
    """显示爬取进度"""
    storage = BatchStorage()
    inc_manager = IncrementalManager()
    stats = storage.get_statistics()
    
    print(f"\n{'='*60}")
    print(f"📊 数据统计报告")
    print(f"{'='*60}")
    print(f"  【主数据】")
    print(f"    总页数: {stats['total_pages']} 页")
    print(f"    总记录数: {stats['total_records']:,} 条")
    print(f"    批次数: {stats['total_batches']} 个文件")
    print(f"    文件大小: {stats['total_size_mb']:.1f} MB")
    
    print(f"\n  【增量数据】")
    print(f"    增量次数: {len(inc_manager.index['incremental_files'])}")
    print(f"    增量页数: {inc_manager.index['total_pages']}")
    print(f"    增量记录: {inc_manager.index['total_records']:,} 条")
    
    total_records_all = stats['total_records'] + inc_manager.index['total_records']
    print(f"\n  【合计】")
    print(f"    总记录数: {total_records_all:,} 条")
    
    # 显示批次详情
    if storage.get_all_batches():
        print(f"\n📁 主数据批次详情:")
        for batch_idx in storage.get_all_batches():
            batch_info = storage.progress["batches"][batch_idx]
            pages_count = len(batch_info["pages"])
            expected_pages = batch_info["end_page"] - batch_info["start_page"] + 1
            records = batch_info["records"]
            file_path = Path(stats['data_dir']) / batch_info["file"]
            file_size = file_path.stat().st_size / (1024 * 1024) if file_path.exists() else 0
            progress_pct = pages_count / expected_pages * 100
            print(f"  批次 {batch_idx:03d}: {pages_count}/{expected_pages}页 ({progress_pct:.0f}%), {records:,}条, {file_size:.2f}MB")
    
    # 显示增量历史
    if inc_manager.index['incremental_files']:
        print(f"\n📁 增量数据历史:")
        for i, inc_file in enumerate(inc_manager.index['incremental_files'][-5:], 1):
            print(f"    {i}. {inc_file['time'][:19]} - {inc_file['pages']}页, {inc_file['records']}条")
    
    print(f"{'='*60}")


# ================= 查看增量历史 =================
def show_incremental_history():
    """显示增量爬取历史"""
    inc_manager = IncrementalManager()
    inc_manager.show_incremental_history()


# ================= 合并增量数据 =================
def merge_incremental_to_main():
    """
    将增量数据合并到主数据（慎用）
    当增量数据积累较多时，可以合并到主数据
    """
    storage = BatchStorage()
    inc_manager = IncrementalManager()
    
    print(f"\n{'='*60}")
    print(f"⚠️ 合并增量数据到主数据")
    print(f"{'='*60}")
    print(f"  当前增量数据: {inc_manager.index['total_pages']} 页, {inc_manager.index['total_records']:,} 条")
    print(f"\n  注意: 合并后增量数据将清空，主数据将包含所有数据")
    
    confirm = input(f"\n确认合并？(y/n): ").strip().lower()
    if confirm != 'y':
        print("已取消")
        return
    
    # 这里需要实现合并逻辑
    # 由于新数据在前，需要将主数据整体后移
    print("\n⚠️ 合并功能需要实现数据迁移，暂时跳过")
    print("   建议：保持增量数据单独存储，需要完整数据时使用导出功能")
    
    # 简单方案：导出所有数据
    print("\n💡 替代方案：使用导出功能生成完整CSV文件")


# ================= 导出所有数据（含增量） =================
def export_all_to_csv():
    """导出所有数据（主数据 + 增量数据）到CSV"""
    try:
        import pandas as pd
    except ImportError:
        print("❌ 请先安装 pandas: pip install pandas")
        return
    
    storage = BatchStorage()
    inc_manager = IncrementalManager()
    
    stats = storage.get_statistics()
    total_main = stats['total_records']
    total_inc = inc_manager.index['total_records']
    
    print(f"\n📤 开始导出所有数据到CSV...")
    print(f"  主数据: {total_main:,} 条")
    print(f"  增量数据: {total_inc:,} 条")
    print(f"  合计: {total_main + total_inc:,} 条")
    print(f"  可能需要几分钟...")
    
    all_data = []
    
    # 1. 导出主数据
    print(f"\n  正在导出主数据...")
    for record in storage.get_all_data_generator():
        all_data.append(record)
        if len(all_data) % 10000 == 0:
            print(f"    已处理 {len(all_data):,} 条...")
    
    # 2. 导出增量数据
    print(f"\n  正在导出增量数据...")
    for record in inc_manager.get_all_incremental_data_generator():
        all_data.append(record)
        if len(all_data) % 10000 == 0:
            print(f"    已处理 {len(all_data):,} 条...")
    
    # 保存CSV
    df = pd.DataFrame(all_data)
    output_file = f"all_data_export_{datetime.now().strftime('%Y%m%d')}.csv"
    df.to_csv(output_file, index=False, encoding='utf-8-sig')
    
    file_size_mb = Path(output_file).stat().st_size / (1024 * 1024)
    print(f"\n✅ 导出完成: {len(all_data):,}条 -> {output_file} ({file_size_mb:.1f} MB)")


# ================= 测试单页 =================
def test_single_page(page=1):
    """测试单页爬取"""
    print(f"\n🧪 测试模式: 爬取第{page}页 (每页{CONFIG['PAGE_SIZE']}条)\n")
    
    data = get_rowData(page=page, page_size=CONFIG["PAGE_SIZE"])
    
    if data:
        print(f"\n✅ 测试成功！获取到 {len(data)} 条数据")
        
        print(f"\n📋 第一条数据预览:")
        first = data[0]
        for key, value in list(first.items())[:10]:
            if value and len(str(value)) > 50:
                value = str(value)[:50] + "..."
            print(f"  {key}: {value}")
        
        test_file = f"test_page_{page}.json"
        with open(test_file, 'w', encoding='utf-8') as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        print(f"\n💾 测试数据已保存: {test_file}")
        
        return data
    else:
        print(f"\n❌ 测试失败！未获取到数据")
        return None


# ================= 主入口 =================
def main():
    while True:
        print(f"\n{'='*50}")
        print(f"🌾 种业数据爬虫 v3.2 (增量单独存储版)")
        print(f"{'='*50}")
        print(f"  当前速度配置:")
        print(f"    每页间隔: {CONFIG['MIN_INTERVAL']}-{CONFIG['MAX_INTERVAL']} 秒 (随机)")
        print(f"    批次休息: 每50页休息10-20秒")
        print(f"{'='*50}")
        print(f"  【主数据操作】")
        print(f"  1. 测试模式 - 爬取单页")
        print(f"  2. 完整爬取 - 首次全量爬取")
        print(f"  3. 查看统计 - 查看主数据+增量统计")
        print(f"  4. 继续上次 - 断点续传")
        print(f"  【增量数据操作】")
        print(f"  5. 增量爬取 - 只爬取新增数据 ✨")
        print(f"  6. 增量历史 - 查看增量记录")
        print(f"  【导出操作】")
        print(f"  7. 导出CSV - 导出主数据")
        print(f"  8. 导出全部CSV - 导出主数据+增量数据")
        print(f"  0. 退出")
        print(f"{'='*50}")
        
        choice = input("\n请选择 (0-8): ").strip()
        
        if choice == '0':
            print("\n👋 再见！")
            break
        elif choice == '1':
            page = int(input(f"请输入页码 (默认1): ") or "1")
            test_single_page(page)
            input("\n按回车继续...")
        elif choice == '2':
            print(f"\n⚠️  将爬取第1~{CONFIG['MAX_PAGES']}页")
            print(f"  每页{CONFIG['PAGE_SIZE']}条 → 总计{CONFIG['TOTAL_RECORDS']:,}条")
            print(f"  生成文件数: {CONFIG['TOTAL_BATCHES']}个")
            print(f"  预计耗时: ~{CONFIG['MAX_PAGES'] * (CONFIG['MIN_INTERVAL'] + CONFIG['MAX_INTERVAL']) / 2 / 60:.1f} 分钟")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            
            if confirm == 'y':
                crawl_batch(start_page=1, end_page=CONFIG["MAX_PAGES"])
                input("\n按回车继续...")
            else:
                print("已取消")
        elif choice == '3':
            show_progress()
            input("\n按回车继续...")
        elif choice == '4':
            storage = BatchStorage()
            next_page = storage.get_next_page()
            
            if next_page and next_page <= CONFIG["MAX_PAGES"]:
                print(f"\n📌 找到上次进度，从第{next_page}页继续")
                confirm = input("  确认继续？(y/n): ").strip().lower()
                
                if confirm == 'y':
                    crawl_batch(start_page=next_page, end_page=CONFIG["MAX_PAGES"])
                    input("\n按回车继续...")
            else:
                print("✅ 所有页面已爬取完成！")
                input("\n按回车继续...")
        elif choice == '5':
            incremental_crawl()
            input("\n按回车继续...")
        elif choice == '6':
            show_incremental_history()
            input("\n按回车继续...")
        elif choice == '7':
            export_to_csv()
            input("\n按回车继续...")
        elif choice == '8':
            export_all_to_csv()
            input("\n按回车继续...")
        else:
            print("❌ 无效选择")
            time.sleep(1)


# 保留原有的导出函数
def export_to_csv():
    """导出主数据到CSV"""
    try:
        import pandas as pd
    except ImportError:
        print("❌ 请先安装 pandas: pip install pandas")
        return
    
    storage = BatchStorage()
    stats = storage.get_statistics()
    
    print(f"\n📤 开始导出主数据到CSV...")
    print(f"  共 {stats['total_records']:,} 条数据，可能需要几分钟")
    
    all_data = []
    for record in storage.get_all_data_generator():
        all_data.append(record)
        if len(all_data) % 10000 == 0:
            print(f"  已处理 {len(all_data):,} 条...")
    
    df = pd.DataFrame(all_data)
    output_file = f"main_data_export_{datetime.now().strftime('%Y%m%d')}.csv"
    df.to_csv(output_file, index=False, encoding='utf-8-sig')
    
    file_size_mb = Path(output_file).stat().st_size / (1024 * 1024)
    print(f"\n✅ 导出完成: {len(all_data):,}条 -> {output_file} ({file_size_mb:.1f} MB)")


if __name__ == "__main__":
    main()
```



## 数据入库测试

增量改进版：

```python
# -*- coding: utf-8 -*-
"""
JSON数据批量导入MySQL
支持分批导入，内存友好
支持测试模式（先导入少量数据验证）
✨ 新增：支持增量导入（基于 varietyannouncementid 去重）
✨ 新增：支持测试表 variety_data_test
"""

import json
import pymysql
from pathlib import Path
from datetime import datetime

# ================= 数据库配置 =================
DB_CONFIG = {
    'host': '192.168.10.224',
    'port': 3306,
    'user': 'yunfeng',
    'password': '386898136Di!',
    'database': 'yunfeng',
    'charset': 'utf8mb4'
}

# ================= 数据目录 =================
DATA_DIR = "variety_batch_data"  # 爬虫保存的JSON数据目录
INCREMENTAL_DIR = "incremental_data"  # 增量数据目录

# ================= 表配置 =================
# 默认使用测试表，可以切换
USE_TEST_TABLE = True  # True: 使用 variety_data_test, False: 使用 variety_data
TEST_TABLE_NAME = "variety_data_test"
PROD_TABLE_NAME = "variety_data"


def get_table_name():
    """获取当前使用的表名"""
    return TEST_TABLE_NAME if USE_TEST_TABLE else PROD_TABLE_NAME


def get_connection():
    """获取数据库连接"""
    return pymysql.connect(**DB_CONFIG)


def clean_value(value):
    """清理单个值，处理None和特殊字符"""
    if value is None:
        return None
    if isinstance(value, str):
        # 转义单引号，避免SQL注入
        return value.strip().replace("'", "\\'")
    if isinstance(value, int):
        return value
    return str(value)


def parse_record(record):
    """
    将JSON记录解析为数据库字段
    返回: 按表字段顺序的值列表（23个字段，不含id和时间戳）
    """
    return (
        clean_value(record.get('cropid')),
        clean_value(record.get('cropname')),
        clean_value(record.get('varietyname')),
        clean_value(record.get('varietyannouncementid')),
        clean_value(record.get('judgementno')),
        clean_value(record.get('judgementyear')),
        clean_value(record.get('judgementregion')),
        clean_value(record.get('judgementregionid')),
        clean_value(record.get('certificateno')),
        clean_value(record.get('breedingcompany')),
        clean_value(record.get('applycompany')),
        clean_value(record.get('varietycharacter')),
        clean_value(record.get('varietysource')),
        clean_value(record.get('plantrequirment')),
        clean_value(record.get('judgementsuggestion')),
        clean_value(record.get('outputexpression')),
        clean_value(record.get('isTuiguangHas', 0)),
        clean_value(record.get('isVarietyApplyHas', 0)),
        clean_value(record.get('isXukeByVarietynameHas', 0)),
        clean_value(record.get('isXukeByApplycompanynameHas', 0)),
        clean_value(record.get('istransgenosis')),
        clean_value(record.get('status', 1)),
    )


def load_existing_announcement_ids(cursor, table_name):
    """
    从指定表加载已存在的公告ID
    用于增量导入时的去重判断（基于 varietyannouncementid）
    """
    query = f"SELECT varietyannouncementid FROM {table_name} WHERE varietyannouncementid IS NOT NULL"
    cursor.execute(query)
    results = cursor.fetchall()
    return set(row[0] for row in results)


def create_test_table_if_not_exists(cursor):
    """如果测试表不存在，则创建测试表（结构与正式表相同）"""
    create_sql = """
    CREATE TABLE IF NOT EXISTS variety_data_test (
        `id` int NOT NULL AUTO_INCREMENT COMMENT '自增主键',
        `cropid` int DEFAULT NULL COMMENT '作物ID',
        `cropname` varchar(50) DEFAULT NULL COMMENT '作物名称',
        `varietyname` varchar(200) DEFAULT NULL COMMENT '品种名称',
        `varietyannouncementid` varchar(36) NOT NULL COMMENT '公告ID(UUID)',
        `judgementno` varchar(100) DEFAULT NULL COMMENT '审定编号',
        `judgementyear` int DEFAULT NULL COMMENT '审定年份',
        `judgementregion` varchar(100) DEFAULT NULL COMMENT '审定区域',
        `judgementregionid` varchar(20) DEFAULT NULL COMMENT '区域代码',
        `certificateno` varchar(100) DEFAULT NULL COMMENT '证书编号',
        `breedingcompany` text COMMENT '育种单位',
        `applycompany` text COMMENT '申请单位',
        `varietycharacter` text COMMENT '品种特性',
        `varietysource` text COMMENT '品种来源',
        `plantrequirment` text COMMENT '种植要求',
        `judgementsuggestion` text COMMENT '审定建议',
        `outputexpression` text COMMENT '产量表现',
        `isTuiguangHas` tinyint DEFAULT '0',
        `isVarietyApplyHas` tinyint DEFAULT '0',
        `isXukeByVarietynameHas` tinyint DEFAULT '0',
        `isXukeByApplycompanynameHas` tinyint DEFAULT '0',
        `istransgenosis` tinyint DEFAULT NULL,
        `status` tinyint DEFAULT '1',
        `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
        `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
        PRIMARY KEY (`id`),
        UNIQUE KEY `uk_announcement` (`varietyannouncementid`),
        KEY `idx_variety_name` (`varietyname`(50)),
        KEY `idx_judgement_year` (`judgementyear`),
        KEY `idx_crop_year` (`cropid`,`judgementyear`),
        KEY `idx_region` (`judgementregion`(50)),
        FULLTEXT KEY `ft_character` (`varietycharacter`) /*!50100 WITH PARSER `ngram` */,
        FULLTEXT KEY `ft_suggestion` (`judgementsuggestion`) /*!50100 WITH PARSER `ngram` */
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='品种原始数据测试表'
    """
    try:
        cursor.execute(create_sql)
        print(f"✅ 测试表 {TEST_TABLE_NAME} 已准备就绪")
    except Exception as e:
        print(f"⚠️ 创建测试表时出现问题: {e}")
        print("   可能表已存在，继续执行...")


def clear_test_table(cursor):
    """清空测试表数据"""
    try:
        cursor.execute(f"TRUNCATE TABLE {TEST_TABLE_NAME}")
        print(f"✅ 测试表 {TEST_TABLE_NAME} 已清空")
    except Exception as e:
        print(f"❌ 清空测试表失败: {e}")


def import_batch(records, cursor, batch_size=500):
    """
    批量导入记录（使用 INSERT IGNORE，配合唯一索引）
    """
    table_name = get_table_name()
    sql = f"""
        INSERT IGNORE INTO {table_name} (
            cropid, cropname, varietyname, varietyannouncementid,
            judgementno, judgementyear, judgementregion, judgementregionid,
            certificateno, breedingcompany, applycompany,
            varietycharacter, varietysource, plantrequirment,
            judgementsuggestion, outputexpression,
            isTuiguangHas, isVarietyApplyHas, isXukeByVarietynameHas,
            isXukeByApplycompanynameHas, istransgenosis, status
        ) VALUES (
            %s, %s, %s, %s,
            %s, %s, %s, %s,
            %s, %s, %s,
            %s, %s, %s,
            %s, %s,
            %s, %s, %s,
            %s, %s, %s
        )
    """
    
    inserted = 0
    for i in range(0, len(records), batch_size):
        batch = records[i:i+batch_size]
        try:
            cursor.executemany(sql, batch)
            # rowcount 返回实际影响的行数（INSERT IGNORE 会跳过重复）
            inserted += cursor.rowcount
            print(f"    ✅ 已入库: {inserted}/{len(records)} 条")
        except Exception as e:
            print(f"    ❌ 批次入库失败: {e}")
            # 降级：逐条插入
            for record in batch:
                try:
                    cursor.execute(sql, record)
                    if cursor.rowcount > 0:
                        inserted += 1
                except Exception as e2:
                    print(f"      ❌ 单条失败: {record[3]}, 错误: {e2}")
    
    return inserted


def import_incremental_batch(records, cursor, existing_ids, batch_size=500):
    """
    增量批量导入 - 只导入新的记录（基于 varietyannouncementid）
    """
    # 过滤出新记录
    new_records = []
    for record in records:
        announcement_id = record[3]  # varietyannouncementid 在第4个字段（索引3）
        if announcement_id and announcement_id not in existing_ids:
            new_records.append(record)
    
    if not new_records:
        return 0, 0
    
    print(f"  📊 发现新数据: {len(new_records)} 条")
    
    table_name = get_table_name()
    sql = f"""
        INSERT INTO {table_name} (
            cropid, cropname, varietyname, varietyannouncementid,
            judgementno, judgementyear, judgementregion, judgementregionid,
            certificateno, breedingcompany, applycompany,
            varietycharacter, varietysource, plantrequirment,
            judgementsuggestion, outputexpression,
            isTuiguangHas, isVarietyApplyHas, isXukeByVarietynameHas,
            isXukeByApplycompanynameHas, istransgenosis, status
        ) VALUES (
            %s, %s, %s, %s,
            %s, %s, %s, %s,
            %s, %s, %s,
            %s, %s, %s,
            %s, %s,
            %s, %s, %s,
            %s, %s, %s
        )
    """
    
    inserted = 0
    for i in range(0, len(new_records), batch_size):
        batch = new_records[i:i+batch_size]
        try:
            cursor.executemany(sql, batch)
            inserted += len(batch)
            print(f"    ✅ 已入库新数据: {inserted}/{len(new_records)} 条")
        except Exception as e:
            print(f"    ❌ 批次入库失败: {e}")
            # 降级：逐条插入
            for record in batch:
                try:
                    cursor.execute(sql, record)
                    inserted += 1
                except Exception as e2:
                    print(f"      ❌ 单条失败: {record[3]}, 错误: {e2}")
    
    return inserted, len(new_records)


def load_records_from_files(data_dir, max_records=None, load_all_batches=True):
    """
    从JSON文件加载记录
    max_records: 最大加载条数（用于测试）
    load_all_batches: 是否加载所有批次（False表示只加载最新的批次）
    """
    data_path = Path(data_dir)
    batch_files = sorted(data_path.glob("batch_*.json"))
    
    if not batch_files:
        print(f"❌ 未找到JSON文件: {data_dir}")
        return []
    
    # 如果不是加载所有批次，只取最后一个
    if not load_all_batches and len(batch_files) > 1:
        batch_files = batch_files[-1:]
        print(f"📂 增量模式: 只检查最新批次文件")
    
    all_records = []
    
    for batch_file in batch_files:
        print(f"  加载: {batch_file.name}")
        with open(batch_file, 'r', encoding='utf-8') as f:
            batch_data = json.load(f)
        
        # 展开所有记录（按页码排序）
        for page_num in sorted(batch_data.keys(), key=int):
            page_records = batch_data[page_num]
            for record in page_records:
                all_records.append(record)
                if max_records and len(all_records) >= max_records:
                    return all_records
    
    return all_records


def load_incremental_records(inc_dir=INCREMENTAL_DIR):
    """
    从增量数据目录加载增量数据（爬虫生成的增量文件）
    """
    inc_path = Path(inc_dir)
    if not inc_path.exists():
        print(f"❌ 未找到增量数据目录: {inc_dir}")
        return []
    
    inc_files = sorted(inc_path.glob("incremental_*.json"))
    if not inc_files:
        print(f"⚠️ 未找到增量数据文件")
        return []
    
    all_records = []
    
    for inc_file in inc_files:
        print(f"  加载增量文件: {inc_file.name}")
        with open(inc_file, 'r', encoding='utf-8') as f:
            inc_data = json.load(f)
        
        # 增量文件结构: {"data": {page_num: page_data, ...}}
        page_data_dict = inc_data.get("data", {})
        for page_num, page_records in page_data_dict.items():
            for record in page_records:
                all_records.append(record)
    
    return all_records


def import_from_json_files(data_dir=DATA_DIR, max_records=None, dry_run=False, incremental=False):
    """
    从JSON文件导入数据
    max_records: 最大导入条数（None表示全部）
    dry_run: 是否为试运行（只读取不写入）
    incremental: 是否为增量导入（只导入新数据，基于 varietyannouncementid）
    """
    table_name = get_table_name()
    
    print(f"\n📂 正在加载数据...")
    
    # 加载记录
    records = load_records_from_files(data_dir, max_records, load_all_batches=not incremental)
    
    if not records:
        print(f"❌ 未找到数据")
        return 0
    
    print(f"📊 共加载 {len(records)} 条记录")
    
    if dry_run:
        print(f"\n🔍 试运行模式（不实际写入数据库）")
        print(f"   目标表: {table_name}")
        print(f"   前3条数据预览:")
        for i, record in enumerate(records[:3]):
            print(f"\n   第{i+1}条:")
            print(f"     varietyannouncementid: {record.get('varietyannouncementid')}")
            print(f"     judgementno: {record.get('judgementno')}")
            print(f"     varietyname: {record.get('varietyname')}")
        return 0
    
    print(f"\n{'='*60}")
    mode = "增量导入" if incremental else "全量导入"
    print(f"🚀 开始{mode}数据到MySQL")
    print(f"{'='*60}")
    print(f"📂 数据目录: {data_dir}")
    print(f"📊 目标表: {table_name}")
    print(f"📊 待导入: {len(records)} 条")
    if incremental:
        print(f"✨ 模式: 基于公告ID(varietyannouncementid)去重，只插入新数据")
    print(f"{'='*60}\n")
    
    conn = get_connection()
    cursor = conn.cursor()
    
    # 如果是测试表，确保表存在
    if USE_TEST_TABLE:
        create_test_table_if_not_exists(cursor)
    
    # 解析记录
    parsed_records = []
    skipped_no_id = 0
    for record in records:
        parsed = parse_record(record)
        if parsed[3]:  # varietyannouncementid
            parsed_records.append(parsed)
        else:
            skipped_no_id += 1
    
    if skipped_no_id > 0:
        print(f"⚠️ 跳过 {skipped_no_id} 条缺少公告ID的记录\n")
    
    print(f"✅ 有效记录: {len(parsed_records)} 条\n")
    
    # 增量或全量导入
    if incremental:
        # 加载已存在的公告ID
        existing_ids = load_existing_announcement_ids(cursor, table_name)
        print(f"📊 {table_name} 中已有记录数: {len(existing_ids):,} 条")
        
        # 增量导入
        inserted, new_count = import_incremental_batch(parsed_records, cursor, existing_ids)
    else:
        # 全量导入（使用 INSERT IGNORE 避免重复）
        inserted = import_batch(parsed_records, cursor)
        new_count = inserted
    
    conn.commit()
    
    cursor.close()
    conn.close()
    
    print(f"\n{'='*60}")
    print(f"🎉 导入完成！")
    print(f"{'='*60}")
    print(f"  目标表: {table_name}")
    print(f"  总记录数: {len(parsed_records)}")
    print(f"  成功导入: {inserted}")
    if incremental:
        print(f"  新增数据: {new_count}")
        print(f"  已存在跳过: {len(parsed_records) - new_count}")
    print(f"{'='*60}")
    
    return inserted


def import_incremental_from_crawler(inc_dir=INCREMENTAL_DIR):
    """
    从爬虫的增量数据目录导入增量数据
    """
    table_name = get_table_name()
    
    print(f"\n📂 正在加载增量数据...")
    
    # 加载增量数据
    records = load_incremental_records(inc_dir)
    
    if not records:
        print(f"❌ 未找到增量数据")
        return 0
    
    print(f"📊 共加载 {len(records)} 条增量记录")
    
    print(f"\n{'='*60}")
    print(f"🚀 开始导入增量数据到MySQL")
    print(f"{'='*60}")
    print(f"📂 增量目录: {inc_dir}")
    print(f"📊 目标表: {table_name}")
    print(f"📊 待导入: {len(records)} 条")
    print(f"✨ 模式: 基于公告ID(varietyannouncementid)自动去重")
    print(f"{'='*60}\n")
    
    conn = get_connection()
    cursor = conn.cursor()
    
    # 如果是测试表，确保表存在
    if USE_TEST_TABLE:
        create_test_table_if_not_exists(cursor)
    
    # 解析记录
    parsed_records = []
    skipped_no_id = 0
    for record in records:
        parsed = parse_record(record)
        if parsed[3]:  # varietyannouncementid
            parsed_records.append(parsed)
        else:
            skipped_no_id += 1
    
    if skipped_no_id > 0:
        print(f"⚠️ 跳过 {skipped_no_id} 条缺少公告ID的记录\n")
    
    print(f"✅ 有效记录: {len(parsed_records)} 条\n")
    
    # 加载已存在的公告ID
    existing_ids = load_existing_announcement_ids(cursor, table_name)
    print(f"📊 {table_name} 中已有记录数: {len(existing_ids):,} 条")
    
    # 增量导入
    inserted, new_count = import_incremental_batch(parsed_records, cursor, existing_ids)
    
    conn.commit()
    
    cursor.close()
    conn.close()
    
    print(f"\n{'='*60}")
    print(f"🎉 增量导入完成！")
    print(f"{'='*60}")
    print(f"  目标表: {table_name}")
    print(f"  总增量记录: {len(parsed_records)}")
    print(f"  新增记录: {new_count}")
    print(f"  已存在跳过: {len(parsed_records) - new_count}")
    print(f"{'='*60}")
    
    return inserted


def verify_import():
    """验证导入结果"""
    table_name = get_table_name()
    conn = get_connection()
    cursor = conn.cursor()
    
    # 检查表是否存在
    cursor.execute(f"SHOW TABLES LIKE '{table_name}'")
    if not cursor.fetchone():
        print(f"\n⚠️ 表 {table_name} 不存在")
        cursor.close()
        conn.close()
        return
    
    # 统计总数
    cursor.execute(f"SELECT COUNT(*) FROM {table_name}")
    total = cursor.fetchone()[0]
    
    if total == 0:
        print(f"\n⚠️ {table_name} 表中暂无数据")
        cursor.close()
        conn.close()
        return
    
    # 按作物统计
    cursor.execute(f"""
        SELECT cropname, COUNT(*) as count 
        FROM {table_name}
        WHERE cropname IS NOT NULL
        GROUP BY cropname 
        ORDER BY count DESC
        LIMIT 10
    """)
    crop_stats = cursor.fetchall()
    
    # 按年份统计
    cursor.execute(f"""
        SELECT judgementyear, COUNT(*) as count 
        FROM {table_name}
        WHERE judgementyear IS NOT NULL
        GROUP BY judgementyear 
        ORDER BY judgementyear DESC
        LIMIT 10
    """)
    year_stats = cursor.fetchall()
    
    # 最近5条数据
    cursor.execute(f"""
        SELECT varietyannouncementid, varietyname, cropname, judgementyear 
        FROM {table_name}
        ORDER BY created_at DESC 
        LIMIT 5
    """)
    recent = cursor.fetchall()
    
    cursor.close()
    conn.close()
    
    print(f"\n{'='*60}")
    print(f"📊 数据验证结果 - {table_name}")
    print(f"{'='*60}")
    print(f"总记录数: {total:,} 条")
    
    if crop_stats:
        print(f"\n按作物统计（前10）:")
        for row in crop_stats:
            print(f"  {row[0]}: {row[1]:,} 条")
    
    if year_stats:
        print(f"\n按年份统计（最近10年）:")
        for row in year_stats:
            print(f"  {row[0]}年: {row[1]:,} 条")
    
    if recent:
        print(f"\n最近入库的5条数据:")
        for row in recent:
            print(f"  {row[0]} | {row[1]} | {row[2]} | {row[3]}年")
    
    print(f"{'='*60}")


def clear_current_table():
    """清空当前表数据（危险操作）"""
    table_name = get_table_name()
    print(f"\n⚠️  警告：即将清空 {table_name} 表的所有数据！")
    confirm = input(f"  确认清空 {table_name}？(yes/no): ").strip().lower()
    
    if confirm == 'yes':
        conn = get_connection()
        cursor = conn.cursor()
        try:
            cursor.execute(f"TRUNCATE TABLE {table_name}")
            conn.commit()
            print(f"✅ 表 {table_name} 已清空")
        except Exception as e:
            print(f"❌ 清空失败: {e}")
        finally:
            cursor.close()
            conn.close()
    else:
        print("已取消")


def show_table_info():
    """显示当前表结构信息"""
    table_name = get_table_name()
    conn = get_connection()
    cursor = conn.cursor()
    
    # 检查表是否存在
    cursor.execute(f"SHOW TABLES LIKE '{table_name}'")
    if not cursor.fetchone():
        print(f"\n⚠️ 表 {table_name} 不存在")
        cursor.close()
        conn.close()
        return
    
    cursor.execute(f"DESCRIBE {table_name}")
    columns = cursor.fetchall()
    
    print(f"\n{'='*60}")
    print(f"📋 表结构信息 - {table_name}")
    print(f"{'='*60}")
    print(f"{'字段名':<30} {'类型':<20} {'允许NULL':<10}")
    print(f"{'-'*60}")
    for col in columns:
        print(f"{col[0]:<30} {col[1]:<20} {col[2]:<10}")
    
    # 显示记录数
    cursor.execute(f"SELECT COUNT(*) FROM {table_name}")
    count = cursor.fetchone()[0]
    print(f"\n当前记录数: {count:,} 条")
    
    cursor.close()
    conn.close()
    print(f"{'='*60}")


def import_from_single_json(json_file="all_variety_data.json", max_records=None, incremental=False):
    """
    从单个合并的JSON文件导入
    max_records: 最大导入条数（None表示全部）
    incremental: 是否为增量导入
    """
    table_name = get_table_name()
    
    print(f"\n📂 正在加载文件: {json_file}")
    
    with open(json_file, 'r', encoding='utf-8') as f:
        records = json.load(f)
    
    if max_records:
        records = records[:max_records]
    
    print(f"📊 共 {len(records)} 条记录")
    
    conn = get_connection()
    cursor = conn.cursor()
    
    # 如果是测试表，确保表存在
    if USE_TEST_TABLE:
        create_test_table_if_not_exists(cursor)
    
    # 解析记录
    parsed_records = []
    skipped_no_id = 0
    for record in records:
        parsed = parse_record(record)
        if parsed[3]:  # varietyannouncementid
            parsed_records.append(parsed)
        else:
            skipped_no_id += 1
    
    print(f"有效记录: {len(parsed_records)} 条")
    if skipped_no_id > 0:
        print(f"跳过(无ID): {skipped_no_id} 条")
    
    # 增量或全量导入
    if incremental:
        existing_ids = load_existing_announcement_ids(cursor, table_name)
        print(f"{table_name} 中已有: {len(existing_ids):,} 条")
        inserted, new_count = import_incremental_batch(parsed_records, cursor, existing_ids)
        print(f"新增记录: {new_count} 条")
    else:
        inserted = import_batch(parsed_records, cursor)
    
    conn.commit()
    
    cursor.close()
    conn.close()
    
    print(f"\n✅ 导入完成: 成功 {inserted}/{len(parsed_records)} 条")
    
    return inserted


def switch_table():
    """切换目标表"""
    global USE_TEST_TABLE
    USE_TEST_TABLE = not USE_TEST_TABLE
    current = "测试表 (variety_data_test)" if USE_TEST_TABLE else "正式表 (variety_data)"
    print(f"\n✅ 已切换到: {current}")


def main():
    global USE_TEST_TABLE
    
    while True:
        current_table = "测试表 (variety_data_test)" if USE_TEST_TABLE else "正式表 (variety_data)"
        
        print(f"\n{'='*60}")
        print(f"🗄️  JSON数据入库工具 v3.0 (支持增量)")
        print(f"{'='*60}")
        print(f"  📍 当前目标表: {current_table}")
        print(f"{'='*60}")
        print(f"  【表切换】")
        print(f"  0. 切换目标表 (测试表/正式表)")
        print(f"  【全量导入】")
        print(f"  1. 全量导入 - 从分批文件导入全部数据")
        print(f"  2. 全量导入 - 从合并文件导入全部数据")
        print(f"  【增量导入】✨")
        print(f"  3. 增量导入 - 从分批文件只导入新数据")
        print(f"  4. 增量导入 - 从爬虫增量目录导入")
        print(f"  5. 增量导入 - 从合并文件只导入新数据")
        print(f"  【测试模式】")
        print(f"  6. 测试模式 - 导入前100条数据")
        print(f"  7. 测试模式 - 导入前500条数据")
        print(f"  8. 测试模式 - 导入前1000条数据")
        print(f"  【工具】")
        print(f"  9. 验证导入结果")
        print(f"  10. 查看表结构")
        print(f"  11. 清空当前表数据（危险）")
        print(f"  12. 退出")
        print(f"{'='*60}")
        
        choice = input("\n请选择 (0-12): ").strip()
        
        # 切换表
        if choice == '0':
            switch_table()
            continue
        
        # 退出
        elif choice == '12':
            print("\n👋 再见！")
            break
        
        # 全量导入
        elif choice == '1':
            print(f"\n🚀 全量导入模式：从分批文件导入全部数据")
            print(f"  数据目录: {DATA_DIR}")
            print(f"  目标表: {get_table_name()}")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_json_files(DATA_DIR, incremental=False)
                verify_import()
            else:
                print("已取消")
        
        elif choice == '2':
            json_file = input("JSON文件路径 (默认: all_variety_data.json): ").strip()
            if not json_file:
                json_file = "all_variety_data.json"
            print(f"\n🚀 全量导入模式：从 {json_file} 导入")
            print(f"  目标表: {get_table_name()}")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_single_json(json_file, incremental=False)
                verify_import()
            else:
                print("已取消")
        
        # 增量导入
        elif choice == '3':
            print(f"\n✨ 增量导入模式：从分批文件只导入新数据")
            print(f"  数据目录: {DATA_DIR}")
            print(f"  目标表: {get_table_name()}")
            print(f"  去重依据: 公告ID(varietyannouncementid)")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_json_files(DATA_DIR, incremental=True)
                verify_import()
            else:
                print("已取消")
        
        elif choice == '4':
            print(f"\n✨ 增量导入模式：从爬虫增量目录导入")
            print(f"  增量目录: {INCREMENTAL_DIR}")
            print(f"  目标表: {get_table_name()}")
            print(f"  去重依据: 公告ID(varietyannouncementid)")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_incremental_from_crawler()
                verify_import()
            else:
                print("已取消")
        
        elif choice == '5':
            json_file = input("JSON文件路径 (默认: all_variety_data.json): ").strip()
            if not json_file:
                json_file = "all_variety_data.json"
            print(f"\n✨ 增量导入模式：从 {json_file} 只导入新数据")
            print(f"  目标表: {get_table_name()}")
            print(f"  去重依据: 公告ID(varietyannouncementid)")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_single_json(json_file, incremental=True)
                verify_import()
            else:
                print("已取消")
        
        # 测试模式
        elif choice == '6':
            print(f"\n🧪 测试模式：导入前100条数据")
            print(f"  目标表: {get_table_name()}")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_json_files(DATA_DIR, max_records=100, incremental=True)
                verify_import()
            else:
                print("已取消")
        
        elif choice == '7':
            print(f"\n🧪 测试模式：导入前500条数据")
            print(f"  目标表: {get_table_name()}")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_json_files(DATA_DIR, max_records=500, incremental=True)
                verify_import()
            else:
                print("已取消")
        
        elif choice == '8':
            print(f"\n🧪 测试模式：导入前1000条数据")
            print(f"  目标表: {get_table_name()}")
            confirm = input("  确认开始？(y/n): ").strip().lower()
            if confirm == 'y':
                import_from_json_files(DATA_DIR, max_records=1000, incremental=True)
                verify_import()
            else:
                print("已取消")
        
        # 工具
        elif choice == '9':
            verify_import()
            input("\n按回车继续...")
        
        elif choice == '10':
            show_table_info()
            input("\n按回车继续...")
        
        elif choice == '11':
            clear_current_table()
            input("\n按回车继续...")
        
        else:
            print("❌ 无效选择")
            input("\n按回车继续...")


if __name__ == "__main__":
    # 测试数据库连接
    try:
        test_conn = get_connection()
        test_conn.close()
        print("✅ 数据库连接成功")
        print(f"📊 当前目标表: {get_table_name()}")
        print(f"🔑 唯一索引: uk_announcement (varietyannouncementid)")
        print(f"\n💡 提示: 默认使用测试表 variety_data_test")
        print(f"   可以随时按 '0' 切换目标表\n")
    except Exception as e:
        print(f"❌ 数据库连接失败: {e}")
        print("\n请检查数据库配置")
        exit(1)
    
    main()
```



### 此时，全量数据已经入库variety_data（_test）

设计一个可执行逻辑，把 `variety_data` 表中的：

- 育种单位 `breedingcompany`
- 申请单位 `applycompany`

拆解、标准化后：

同步到：

- `company` 公司主表
- `variety_company` 关系表

```js
const mysql = require("mysql2/promise");

const BATCH_SIZE = 500;

(async () => {
  const conn = await mysql.createConnection({
    host: "192.168.10.224",
    user: "yunfeng",
    password: "386898136Di!",
    database: "yunfeng"
  });

  console.log("🚀 ETL START");

  // ==========================
  // 1. 读取 ETL 游标（关键新增）
  // ==========================
  const [stateRows] = await conn.execute(
    "SELECT last_variety_id FROM etl_state WHERE id = 1"
  );

  const lastId = stateRows[0].last_variety_id;
  console.log("📍 上次同步ID:", lastId);

  // ==========================
  // 2. 增量读取（核心修改点）
  // ==========================
  const [rows] = await conn.execute(
    "SELECT * FROM variety_data WHERE id > ? ORDER BY id ASC",
    [lastId]
  );

  if (rows.length === 0) {
    console.log("📭 无新增数据，ETL结束");
    await conn.end();
    return;
  }

  console.log(`📦 本次新增数据量: ${rows.length}`);

  // ==========================
  // 3. 内存结构
  // ==========================
  const companyMap = new Map();
  const companySet = new Set();
  const relationSet = new Set();

  // ==========================
  // 4. 已有 company
  // ==========================
  const [companies] = await conn.execute("SELECT * FROM company");

  companies.forEach(c => {
    companyMap.set(c.company_name, c.company_id);
  });

  const newCompanies = [];

  // ==========================
  // 5. ETL 主逻辑
  // ==========================
  let maxId = lastId;

  for (const item of rows) {

    maxId = Math.max(maxId, item.id);

    const varietyId = item.varietyannouncementid;

    const parse = (str, type) => {
      if (!str) return;

      const list = str.split("、");

      for (const name of list) {
        const clean = name.trim();
        if (!clean) continue;

        // company
        if (!companyMap.has(clean) && !companySet.has(clean)) {
          companySet.add(clean);
          newCompanies.push(clean);
        }

        // relation
        relationSet.add(`${varietyId}|${clean}|${type}`);
      }
    };

    parse(item.breedingcompany, 1);
    parse(item.applycompany, 2);
  }

  // ==========================
  // 6. 插入 company
  // ==========================
  console.log(`🏢 新公司数量: ${newCompanies.length}`);

  for (let i = 0; i < newCompanies.length; i += BATCH_SIZE) {
    const batch = newCompanies.slice(i, i + BATCH_SIZE);
    const values = batch.map(name => [name]);

    await conn.query(
      "INSERT IGNORE INTO company (company_name) VALUES ?",
      [values]
    );
  }

  // ==========================
  // 7. 更新 companyMap
  // ==========================
  const [updatedCompanies] = await conn.execute("SELECT * FROM company");

  companyMap.clear();
  updatedCompanies.forEach(c => {
    companyMap.set(c.company_name, c.company_id);
  });

  // ==========================
  // 8. 插入 relation
  // ==========================
  console.log(`🔗 relation数量: ${relationSet.size}`);

  const relationBatch = [];

  for (const key of relationSet) {
    const [varietyId, name, type] = key.split("|");
    const companyId = companyMap.get(name);

    if (!companyId) continue;

    relationBatch.push([
      varietyId,
      companyId,
      Number(type)
    ]);
  }

  for (let i = 0; i < relationBatch.length; i += BATCH_SIZE) {
    const batch = relationBatch.slice(i, i + BATCH_SIZE);

    await conn.query(
      `INSERT IGNORE INTO variety_company 
       (varietyannouncementid, company_id, company_type) 
       VALUES ?`,
      [batch]
    );
  }

  // ==========================
  // 9. 回写游标（核心新增）
  // ==========================
  await conn.execute(
    "UPDATE etl_state SET last_variety_id = ? WHERE id = 1",
    [maxId]
  );

  console.log("📌 更新游标:", maxId);

  console.log("✅ ETL COMPLETE");

  await conn.end();

})().catch(error => {
  console.error("❌ ETL 失败:", error);
  process.exit(1);
});
```



## 然后，系统性的对原始入库数据表进行数据拆分和转移：

包括：

1. 将其中的 cropname 和 cropid 字段的映射关系独立成表；

2. region_name 和 region_code 独立成表

3.  最核心的 variety_dict 表：

   - 从原始数据表往 variety_dict 表迁移数据，确保表内的varietyname字段具有唯一性；
   - 考虑到同一 varietyname 对应了多个不同的审定证号（这符合真实业务逻辑），而不同的审定证号对应着其他唯一数据，例如审定年份，详情描述等；
   - 因此，需要预先保留一个主审定的证号：primary_judgementno，以及相应的：primary_year，以及与之匹配的 description ；

4. 鉴于上诉实际业务逻辑，需要新建 variety_certificate 表，用于存储所有审定证号字段`judgementno`对应的关键数据：

   ```sql
   --
   -- Table structure for table `variety_certificate`
   --
   
   DROP TABLE IF EXISTS `variety_certificate`;
   /*!40101 SET @saved_cs_client     = @@character_set_client */;
   /*!50503 SET character_set_client = utf8mb4 */;
   CREATE TABLE `variety_certificate` (
     `id` int NOT NULL AUTO_INCREMENT COMMENT '证书ID',
     `variety_dict_id` int NOT NULL COMMENT '品种主表ID（variety_dict.id）',
     `judgementno` varchar(100) NOT NULL COMMENT '审定编号',
     `judgementyear` int DEFAULT NULL COMMENT '审定年份',
     `judgementregion` varchar(100) DEFAULT NULL COMMENT '审定区域',
     `judgementregionid` varchar(20) DEFAULT NULL COMMENT '区域代码',
     `certificateno` varchar(100) DEFAULT NULL COMMENT '证书编号',
     `status` tinyint DEFAULT '1' COMMENT '状态（1=有效 0=失效）',
     `is_primary` tinyint DEFAULT '0' COMMENT '是否主证书（1=主证书，用于默认展示）',
     `varietycharacter` text COMMENT '品种特性',
     `created_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP,
     `updated_at` timestamp NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
     PRIMARY KEY (`id`),
     UNIQUE KEY `uk_judgementno` (`judgementno`),
     KEY `idx_variety_dict_id` (`variety_dict_id`),
     KEY `idx_judgementyear` (`judgementyear`),
     KEY `idx_region` (`judgementregionid`),
     CONSTRAINT `fk_certificate_variety` FOREIGN KEY (`variety_dict_id`) REFERENCES `variety_dict` (`id`) ON DELETE CASCADE ON UPDATE CASCADE
   ) ENGINE=InnoDB AUTO_INCREMENT=69730 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='品种审定证书表';
   /*!40101 SET character_set_client = @saved_cs_client */;
   ```

   从原始入库表或经数据清洗后的variety表中，以`judgementno`字段为依据（保证了审定数据的完整性），回填关键信息，

   并以`is_primary`字段标记是否为主审定证号，标记规则为：

   ```sql
   -- 步骤1：重置所有 is_primary = 0
   UPDATE variety_certificate SET is_primary = 0;
   
   -- 步骤2：使用窗口函数计算并更新
   UPDATE variety_certificate vc1
   JOIN (
       SELECT 
           id,
           ROW_NUMBER() OVER (
               PARTITION BY variety_dict_id 
               ORDER BY 
                   CASE WHEN judgementno LIKE '%国%' THEN 0 ELSE 1 END,  -- 包含'国'的排前面
                   judgementyear ASC,  -- 年份旧的在前面
                   judgementno ASC      -- 兜底排序
           ) AS rn
       FROM variety_certificate
   ) AS ranked ON vc1.id = ranked.id AND ranked.rn = 1
   SET vc1.is_primary = 1;
   ```

   与此同时，需要根据`judgementno`字段，对应查询原始表中的`varietyname`，并以此为标的，查询已经创建并完成数据填充的`variety_dict`表当中的自增id，并将结果回填至`variety_certificate`表中的`variety_dict_id`字段

   前端的实际业务需求，需要在确定主审定编号之后，联动查询与之相关的转基因审定编号，因此，需要新建`transgene_variety`表：

   ```sql
   -- 删除旧表（如果存在）
   DROP TABLE IF EXISTS transgene_variety;
   
   -- 使用 INT 类型创建转基因品种独立表
   CREATE TABLE transgene_variety (
     id INT PRIMARY KEY AUTO_INCREMENT,
     variety_dict_id INT NOT NULL COMMENT '品种ID（关联variety_dict.id）',
     parent_variety_id INT COMMENT '对应非转基因品种ID（母本）',
     transgene_type VARCHAR(50) DEFAULT '转基因' COMMENT '转基因类型',
     judgementno_with_transgene VARCHAR(100) COMMENT '含转字的审定证号',
     created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
     updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
     
     INDEX idx_variety_dict_id (variety_dict_id),
     INDEX idx_parent_variety (parent_variety_id),
     UNIQUE KEY uk_variety_dict_id (variety_dict_id),
     FOREIGN KEY (variety_dict_id) REFERENCES variety_dict(id) ON DELETE CASCADE,
     FOREIGN KEY (parent_variety_id) REFERENCES variety_dict(id) ON DELETE SET NULL
   ) COMMENT='转基因品种独立表';
   ```

   前置检查：

   ```sql
   -- 1. 查询 primary_judgementno 包含 '转' 字的品种总数
   SELECT 
     COUNT(*) as total_with_trans_character
   FROM variety_dict
   WHERE primary_judgementno LIKE '%转%'
     AND status = 1
     AND primary_judgementno IS NOT NULL;
   
   -- 2. 查询 transgenic_status = 1 的品种总数
   SELECT 
     COUNT(*) as total_transgenic_status_1
   FROM variety_dict
   WHERE transgenic_status = 1
     AND status = 1;
   
   -- 3. 查询同时满足两个条件的品种总数（交集）
   SELECT 
     COUNT(*) as total_both_conditions
   FROM variety_dict
   WHERE primary_judgementno LIKE '%转%'
     AND transgenic_status = 1
     AND status = 1;
   
   -- 4. 查询 transgenic_status = 1 但 primary_judgementno 不包含 '转' 字的品种
   SELECT 
     COUNT(*) as transgene_status_1_no_trans_char
   FROM variety_dict
   WHERE transgenic_status = 1
     AND status = 1
     AND (primary_judgementno NOT LIKE '%转%' OR primary_judgementno IS NULL);
   
   -- 5. 查询 primary_judgementno 包含 '转' 字但 transgenic_status != 1 的品种
   SELECT 
     COUNT(*) as has_trans_char_not_status_1
   FROM variety_dict
   WHERE primary_judgementno LIKE '%转%'
     AND status = 1
     AND (transgenic_status != 1 OR transgenic_status IS NULL);
   ```

5. 插入转基因作物数据：

   ```sql
   -- ============================================
   -- 完整的数据填充流程
   -- ============================================
   
   -- 1. 清空表
   TRUNCATE TABLE transgene_variety;
   
   -- 2. 插入所有转基因品种（使用 transgenic_status）
   INSERT INTO transgene_variety (variety_dict_id, judgementno_with_transgene, transgene_type)
   SELECT 
     id,
     primary_judgementno,
     '转基因'
   FROM variety_dict
   WHERE transgenic_status = 1
     AND status = 1;
   
   -- 3. 添加 varietyname 字段（方便匹配，或者 JOIN 时获取）
   -- 注意：如果表没有 varietyname 字段，需要添加
   ALTER TABLE transgene_variety ADD COLUMN varietyname VARCHAR(200) AFTER variety_dict_id;
   
   -- 4. 更新品种名称
   UPDATE transgene_variety tv
   JOIN variety_dict vd ON tv.variety_dict_id = vd.id
   SET tv.varietyname = vd.varietyname;
   
   -- 5. 建立关联（去后缀匹配）
   UPDATE transgene_variety tv
   LEFT JOIN variety_dict vd ON (
     -- 去掉末尾的字母后缀
     REGEXP_REPLACE(tv.varietyname, '(D|K|R|TK|ZL|GS|GX|KK)$', '') = vd.varietyname
     -- AND vd.transgenic_status = 0
     AND vd.transgenic_status IN (0, 2)  -- 🔥 修改这里：允许 0 或 2
     AND vd.status = 1
   )
   SET tv.parent_variety_id = vd.id
   WHERE tv.parent_variety_id IS NULL;
   
   -- 6. 查看匹配结果
   SELECT 
     '匹配统计' AS info,
     COUNT(*) AS total,
     SUM(CASE WHEN parent_variety_id IS NOT NULL THEN 1 ELSE 0 END) AS matched,
     SUM(CASE WHEN parent_variety_id IS NULL THEN 1 ELSE 0 END) AS unmatched
   FROM transgene_variety;
   
   -- 7. 查看未匹配的品种（需要人工处理）
   SELECT 
     variety_dict_id,
     varietyname,
     judgementno_with_transgene
   FROM transgene_variety
   WHERE parent_variety_id IS NULL;
   ```

   

